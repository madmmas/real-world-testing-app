{
  "name": "realworld-testing-app",
  "private": true,
  "type": "module",
  "description": "Multi-tenant book marketplace: session UI, JWT APIs, Prisma/Postgres.",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./frontend/*\" dev",
    "dev:frontend": "pnpm -r --parallel --filter \"./frontend/*\" dev",
    "build": "pnpm -r --filter \"./frontend/**\" build",
    "typecheck": "pnpm -r --filter \"./frontend/**\" --filter \"./packages/**\" typecheck",
    "lint": "biome check --diagnostic-level=error frontend packages backend/auth-service backend/user-service backend/catalog-service backend/order-service backend/payment-service backend/store-service backend/inventory-service",
    "openapi:dump": "pnpm --filter @rwa/shared dump-openapi",
    "db:setup": "pnpm --filter @rwa/db db:setup",
    "db:migrate": "pnpm --filter @rwa/db db:migrate",
    "db:seed": "pnpm --filter @rwa/db db:seed"
  },
  "engines": {
    "node": "^22.0.0 || ^24.0.0 || ^26.0.0"
  },
  "packageManager": "pnpm@9.15.9",
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "typescript": "5.8.3"
  }
}
